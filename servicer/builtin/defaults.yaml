git:
  enabled: false
  config:
    user.name: servicer
  default-branch: master
  ignore-unchanged: true
  ignore-servicer-commits: true

environment:
  mappings:
    - branch: master
      environment: production
    - branch: develop
    - branch: env-*
